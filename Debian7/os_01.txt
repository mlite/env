1. su root
2. apt-get install sudo
3. adduser $USER sudo
4. visudo 
   # User privilege specification
   root        ALL=(ALL:ALL) ALL
   newuser    ALL=(ALL:ALL) ALL
5. su $USER
   add the following enviroment to .bashrc
   export PATH=.:$HOME/local/bin:$HOME/.cabal/bin:$PATH
